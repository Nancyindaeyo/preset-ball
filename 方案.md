# 蛾摩拉预设悬浮球 — 需求方案（已拍板稿）

仓库：[`preset-ball`](https://github.com/Nancyindaeyo/preset-ball)  
对照预设：`[MoM]蛾摩拉☫2.6.json`  
内容表：`写卡/预设/蛾摩拉条目重叠与开关.md`  
破限：`写卡/预设/Gemini头部尾部破限.md`  
参考：[ST-XiaoBingKuai](https://github.com/nai040720-svg/ST-XiaoBingKuai)（学交互，不学产品形态）

宿主：SillyTavern 第三方扩展；TauriTavern 同一套 third-party 加载。不写 TT 专用后端。

**主设备：手机 / TT iOS 移动端；电脑为辅。** 美化跟 ST 原生主题变量走。

本文是敲定后的需求。文末只留还没拍板的问题；其余按这里实现。

---

## 0. 已拍板对照

| # | 结论 |
|---|---|
| 1 角色卡用 | 关同人相关，开 `→ 固定思维链`。骨架以后用户自己改。 |
| 2 人设立体 | 两层：伦理四包互斥 + 人设条目可单独开关。 |
| 3 方案内容 | 当前预设 **全量开关快照**。 |
| 4 未绑定聊天 | 切过去 **不改** 开关。 |
| 5 开海棠 NSFW | **不**自动切伦理包。 |
| 6 NSFW 文笔 | 不写死唯美/海棠；记住用户自己的设置。 |
| 7 预设适配 | v1 快捷包只认蛾摩拉 2.6；方案快照按名通用。全预设适配以后再说。 |
| 8 破限 | 要。按破限文档「叠加顺序」做档位，且用户可改每档条目。 |
| 9 MoM 快捷开关 | 你会关掉。本扩展接管条目开关；另做全局世界书快捷挂载。 |
| 10 设备 | 手机 / TT iOS 为主，电脑为辅。 |

新增（本轮）：

- 方案可打开编辑：列出全部条目 → 开关 → 确认保存；可改名。
- 内置规则可改（例如 NSFW 关掉时不再强制开「防止发情」）。
- 用户可新建板块 / 硬互斥 / 一键规则，流程要短、好懂。
- 全局世界书：收藏一本后，点一下挂全局，再点一下摘掉。
- 一键开关是分区接收器，细调里仍要能改里面每条弹药。

---

## 1. 产品是什么

悬浮球管四件事：

1. **方案**：一整份条目开关，可取名、可编辑、可一键套用。玩角色卡 / 原创 / 同人、这个聊天温柔 / 那个聊天黑暗，都靠方案。
2. **叠加与规则**：NSFW、伦理包、少作业、破限档位。点一下按「开这些 / 关那些」执行。规则可改、可自建。
3. **绑聊天**：进入该聊天时自动套绑定的方案。未绑定不动。
4. **全局世界书**：点一下把指定世界书加入 ST 全局世界书，再点摘掉。

细调不是第二套小冰块墙，而是：点进方案编辑、或点开某个板块，才能看到该范围里的每一条。主屏保持大按钮。

---

## 2. 界面原则（移动端优先）

- 悬浮球默认真机右下，避开 Home 条 / 安全区；可贴边半隐。电脑不特别优化位置，能拖即可。
- 点球弹出 **底部面板**（bottom sheet）：圆角、可拉高、内部滚动。不要在球旁边展开一块会被键盘/刘海挡住的小菜单。
- 主屏信息少、按钮大，适合拇指。分组用手风琴，少用多 Tab。
- 颜色、边框、模糊、字体跟 ST 走：`--SmartThemeBodyColor`、`--SmartThemeBorderColor`、`--SmartThemeBlurTintColor`、`--SmartThemeQuoteColor`、`--SmartThemeEmColor` 等。开关长得像酒馆自带的 toggle，不要另做一套皮肤。
- UI 仍挂 Shadow DOM（避免和聊天页 CSS 互打），但把页面上的主题变量拷进 shadow host，这样换酒馆美化会跟着变。
- 不要 `Teleport` 到 `body`（移动端弹层会丢样式）。TT iOS 注意 `env(safe-area-inset-*)`，有 `__TAURITAVERN__.api.layout` 则用它补 inset。

主屏区块（从上到下）：

1. **本聊天**：绑定状态、套用/绑定/解绑。
2. **方案**：列表（一指套用）+ 新建 / 从当前保存 / 编辑 / 改名 / 删除。
3. **快捷**：骨架、伦理四包、人设三条、NSFW 总闸、破限档位、少作业。
4. **世界书**：已收藏的全局书，每本一个开/关。
5. **我的**：用户自建板块和规则。
6. **细调**：按蛾摩拉文件夹展开，改单条；硬互斥组显示为单选。

扩展设置页只留：显示悬浮球、重置球位置、恢复出厂规则。

---

## 3. 方案

### 3.1 是什么

一份 `entries: { 条目名: 开/关 }` 的全量快照 + 显示名。套用 = 按名写回当前 `prompt_order`。找不到的条目跳过并提示，不中断。

快照里没写到的条目（后来新加的、自缝新条）套用时 **保持不动**。保存时再扫进全量。

### 3.2 内置起步（可改、可另存、可恢复出厂）

**原创**

- 开：`→ 固定思维链`
- 关：同人完整、短同人、角色卡思维、自由链、3.6 短链、`♤同人卡优化`、`泡泡宇宙·同人`
- 人设三条常开；`友好反应` / `完美恋人` / `温柔恋爱脑` 关

**同人**

- 开：`→ 同人思维链（完整）`、`♤同人卡优化`、`泡泡宇宙·同人`
- 关：固定链和其他骨架、与泡泡宇宙冲突的基调
- 不强行健康言情包

**角色卡**

- 开：`→ 固定思维链`
- 关：所有同人相关（同人完整 / 短同人 / `♤同人卡优化` / `泡泡宇宙·同人`）
- 其余条目不在起步里强行改；用户之后自己调骨架和库

三份起步只是模板。真正常用的是用户另存出来的「同人用」「黑暗」等。

### 3.3 编辑方案（你要的主路径）

点方案上的 **编辑**：

1. 顶部：名字（可改）。
2. 下面是 **当前预设里所有条目** 的开关列表，初始状态 = 这份方案存着的快照（没有的条目用当前预设状态补上）。
3. 按文件夹/板块分组，可搜索。系统槽（`main`、`charDescription`、`Chat History` 等）默认折叠，需要时再展开。
4. 点条目即改这份草稿的开/关，**此时还不写预设、不写方案**。
5. **确认保存** → 写入该方案。是否立刻套用到当前预设：见文末问题 A。
6. 取消 → 丢草稿。

另外两个入口：

- **从当前保存**：用此刻预设全量开关覆盖该方案（或新建）。
- **另存为**：复制一份再起名。

硬互斥组在编辑器里也是单选：开一条会把同组其他条在草稿里关掉，避免存进一份自相矛盾的方案。

### 3.4 绑聊天

酒馆 `prompt_order` 按 **角色** 存，不按聊天。绑定语义：

> 进入这个聊天时，把绑定方案重新写进当前角色的 prompt_order。

`chatMetadata.preset_ball = { profileId, boundAt }`。方案已删则解绑并提示。未绑定聊天不改开关。

---

## 4. 规则：内置可改，用户可新建

规则是数据，不是写死在代码里的 if。出厂规则来自内容表 / 破限文档；你可以改；可以恢复出厂。

三种，新建时只问这一句：**「这组按钮怎么用？」**

| 用户看到的 | 内部 | 做什么 |
|---|---|---|
| 一组开关 | 板块 `group` | 若干条目，各自开/关。适合「人设三条」这种。 |
| 只能开一个 | 硬互斥 `mutex` | 同组只留一条。适合骨架、基调、文风。 |
| 点一下开一组 | 一键 `pack` | 指定「打开时要开的 / 要关的」；关闭时另一套开/关。适合 NSFW、伦理包、破限一档。 |

新建流程（四步，手机一屏一事）：

1. 起名（例如「防发情不要了」不必，那是改旧规则；新建如「我的海棠包」）。
2. 选上面三种之一。
3. 从当前预设搜条目，勾选进这组。
4. 若是「点一下开一组」：再勾「打开时要关哪些」；可选「关闭时要开哪些 / 要关哪些」。

建好出现在 **我的**。可改名、改条目、删除。删用户规则不影响预设本身。

### 4.1 改现有规则（防止发情那个例子）

出厂 NSFW 总闸：

- **打开**：开 `🔞NSFW思考`、`NSFW一键开关`、文笔（用户上次选的海棠或唯美）、按小说类型尽量对上性向补丁；关 `✘ 防止发情`、`🔞性爱流程`、`♨NSFW优化1/2`。不切伦理包。开海棠时额外关 `♡抗支配`、`❃增加恋爱温度`。
- **关闭**：关思考/风格/流程/优化，并 **关 `NSFW一键开关`**（空接收器不占上下文）；出厂还会 **开回 `✘ 防止发情`**。

你要改成「NSFW 关着也不要防止发情」：打开 NSFW 这条规则的编辑，从「关闭时要打开」里去掉 `✘ 防止发情`，保存。之后总闸关上就不会再把它打开；细调里你仍可单独开它。总闸该开的时候照样能一键开 NSFW。

所有出厂 pack / mutex 都用同一套编辑器。

### 4.2 一键开关仍是接收器

蛾摩拉分区弹药多半是 `setvar`，真正进上下文的是：

- `基础需求一键开关`
- `优化分区一键开关`
- `NSFW一键开关`
- `情感扫描一键开关`
- `缄默法则一键开关`（默认关）
- `功能分区一键开关`
- `蛾摩拉说一键开关`

出厂 pack 会带上对应接收器。细调 / 方案编辑里仍列出分区内每一条，用户可以只开某条弹药、或只开关接收器。界面上接收器放在该板块最上面，标注「不开这个，上面开了也进不去」。

### 4.3 出厂互斥与包（可改）

硬互斥：骨架六条只留一；小说类型；人称；抢话；基调；文风；心理有/无颜色；NSFW 海棠/唯美；性向补丁。

伦理四包（互斥，内容同内容表 §3.1）：健康言情 / 慢热群像 / 强势海棠 / 病娇黑暗。人设三条（性格校准、OOC、抗刻板）是独立板块，默认常开，不跟四包绑死。`友好反应` / `完美恋人` 默认关。

少作业 pack：开 `→ 3.6短思维链`（可留 `💭重现与场景`）；关固定/同人完整/主线/三选一小 CoT。

骨架快捷：原创固定链 / 同人完整 / 少作业短链。切骨架按 pack 改思维链 + 同人库/基调，不动 NSFW、文风、格式栏。

### 4.4 破限档位（可改）

按 [Gemini头部尾部破限.md](../写卡/预设/Gemini头部尾部破限.md) 「叠加顺序」做成 **档位**，不是五条互不相关的开关。选更高档 = 带上更低档的条目，并加上本档新条目；选更低档 = 关掉更高档加出来的那些。

出厂：

| 档 | 名字 | 额外打开 |
|---|---|---|
| 1 | 日常 / 微黄 | `🔹Gemini防429`、`身份定义3`、`🔹无预填底部\|3.5用`（以及 `set-不要关` 保持开） |
| 2 | 非流 NSFW / 拒写 | 加 `🔹Gemini头部` |
| 3 | 流式 NSFW / 中途断 | 加 `🔹底部破限1` |
| 4 | 仍截断 | 先加 `🔹抗流式截断2`；同一档里再点一次才加 `🔹抗流式截断`（插符）。用户可改成「4 一档直接两开」 |

档 5 不是再叠一条 prompt，而是 **检查**：关掉底部 assistant 条目（预填充底部、G 无思维链底部等）。采样参数（频率惩罚 / 存在惩罚归零）是否动手：见文末问题 C。

出厂还关着、不放进档位的：身份定义 2、`✎蛾摩拉回复`、抗非流 2000 字报告、旧 3.7flash 缪斯等。

用户可改每档的条目列表，和改 NSFW 规则同一个编辑器（档位 = 一组有顺序的 pack）。

NSFW 文笔：总闸旁记住用户上次选的海棠或唯美；没选过就先不替用户选文笔，只开思考 + 接收器，文笔在细调里点。

---

## 5. 全局世界书快捷

和方案独立（是否写进方案见文末问题 B）。

- 用户从当前已有世界书里挑一本，加入「快捷挂载」列表，可起别名。
- 按钮亮 = 该书已在 ST **全局世界书**（`selected_world_info` / `world_info.globalSelect`）里。
- 点一下：写入全局并保存设置、刷新世界书多选框。
- 再点：从全局列表拿掉。不删世界书文件，也不动角色卡绑定的那本。
- 多本快捷可以同时亮（ST 全局本来就支持多本）。

不管角色卡世界书、人设世界书；第一版只碰全局。世界书条目内部开关不做（你关了 MoM 快捷开关之后，条目级世界书以后若要再单独立项）。

---

## 6. 数据

全局（方案、规则、世界书快捷、球开关、NSFW 文笔记忆）→ `extension_settings.preset_ball` + `saveSettingsDebounced()`。跨设备同步。

聊天绑定 → `chatMetadata.preset_ball`。

球坐标 / 贴边 → `localStorage`（各屏幕不同）。

不要 `localStorage` 存方案/规则；不要 TT `api.extension.store`（和 ST 不同步）。

草案：

```ts
{
  version: 1,
  orbEnabled: true,
  nsfwStyle: 'haitang' | 'weimei' | null,  // null = 还没选
  profiles: [{ id, name, builtin?, updatedAt, entries: Record<string, boolean> }],
  rules: Rule[],          // 出厂 + 用户；含 mutex / group / pack / 破限档
  loreShortcuts: [{ id, name, worldName }],
}
```

`Rule`：`kind` + 条目名列表 + pack 的 enable/disable（开、关两套）+ 破限档的 `level`。出厂规则带 `builtin` id，便于「恢复这一条出厂」。

---

## 7. 技术要点

- Vue 3 + TS，产物 `dist/index.js`，标准 `manifest.json`。
- 改条目走 `promptManager`（动态 `import('/scripts/openai.js')`），不点预设 DOM，不写死相对路径。
- 按 **条目名** 匹配；2.6 identifier 只作重名消歧。
- 世界书：动态 import `/scripts/world-info.js` 的 `selected_world_info`、`world_names`，改完 `saveSettingsDebounced`，并同步 `#world_info` 多选（有的话）。
- 事件：`CHAT_CHANGED` 套绑定；`OAI_PRESET_CHANGED_AFTER` 重新按名解析。非蛾摩拉时出厂快捷包找不到条目就禁用，方案/自建规则/世界书仍可用。
- persist 防抖：渲染官方列表（若有）→ 刷新 token → 存设置。
- 套用加锁，避免和手点、切聊天打架。

---

## 8. v1 做 / 不做

做：方案 CRUD 与全量编辑器、出厂包与可编辑规则、自建三种规则、聊天绑定、破限档位、全局世界书快捷、ST/TT 加载、移动端底部面板 + 原生主题。

不做：自动读 char 选伦理包；改条目正文；世界书条目内部开关；角色卡/人设世界书挂载；全预设自动适配；和酒馆助手/柏宝书合并成一个球。

---

## 9. 实现顺序

1. host：条目读写、世界书全局读写、主题变量拷贝  
2. 出厂 catalog / 规则数据  
3. 方案全量存取 + 编辑器（搜索、分组、确认保存）  
4. 底部面板：方案 + 快捷 + 细调  
5. 规则编辑器 + 新建向导  
6. 破限档位  
7. 世界书快捷  
8. 聊天绑定  
9. iOS 安全区 / 贴边 / 键盘遮挡  

代码可在手机上点完主路径后再 push。

---

## 10. 已拍板（本轮）

- **A.** 编辑方案两个按钮：「只保存」和「保存并套用」。手机上后者是主按钮。
- **B.** 默认不跟方案绑世界书。方案编辑里有可选开关「套用时同步世界书」，有需要再勾。
- **C.** 不做采样参数归零。第 5 步只提供「关掉底部 assistant」。
- **D.** 多本全局世界书可同时挂。
